# Decisions

Constitution Article VIII: when a design decision is genuinely open, append one line
here — the decision and a one-clause rationale — and keep going. Do not stop work to
ask a question you can answer with a defensible default.

This file exists so the first node to hit a fork does not have to invent a format.

**Precedence.** The `domain-modeling` skill in `.claude/skills/` ships an `ADR-FORMAT.md`
describing numbered ADR files under `docs/adr/`. **That convention is not used in this
repository.** Article VIII of the constitution is the standard, this file is its target,
and the constitution outranks a skill — ergane injects the standards path as binding and
marks its own outer loop authoritative, while skills are advisory. Do not create
`docs/adr/`. If a decision genuinely needs more than one line, raise an escalation.

## Format

One decision per line. Append to the end. Never edit or delete an existing line —
a decision that was later reversed gets a **new** line saying so, because the value
of this file is the trail, not the current state.

```
- YYYY-MM-DD | <spec-id> | <the decision, stated as a choice> — <one-clause rationale>
```

- **`<spec-id>`** is the feature directory the node was working in (`001-scaffold`),
  or `operator` when a human decided it outside a node.
- **The decision** names what was chosen, not what was considered. "Doors are a state
  machine" — not "considered several door approaches".
- **The rationale** is one clause. If it needs a paragraph, it is not a defensible
  default and belongs in an escalation instead.
- Reversals: `— reverses the <date> entry, because <clause>`.

Anything larger than one line — a decision that reshapes the architecture, or one you
cannot defend without prose — is not a DECISIONS entry. Raise it as an escalation and
let the operator answer.

## Log

- 2026-08-29 | operator | DECISIONS.md is a flat append-only line log, not numbered ADR files — Article VIII asks for one line and a rationale, and a directory of documents would invite essays the constitution explicitly does not want.
- 2026-08-29 | operator | Agent skills are vendored into `.claude/skills/` as real files, copied from mattpocock/skills@6654f6b — a node's sandbox gets a factory-owned HOME, so `~/.claude` skills are invisible and the symlinked local copies would dangle.
- 2026-08-29 | operator | `design-an-interface` dropped from the architect persona rather than sourced — it was retired upstream and absorbed into `codebase-design`, which the architect already loads.
- 2026-08-29 | operator | `docs/agents/issue-tracker.md` points findings at the node verdict instead of a tracker — the `code-review` skill requires that file and otherwise instructs the node to run a slash command it has no way to run.
- 2026-08-29 | operator | DECISIONS.md outranks the `domain-modeling` skill's ADR-FORMAT.md, and `docs/adr/` is not used — the constitution is binding on nodes and a vendored skill is advisory, so the contradiction is settled here rather than per-node.
- 2026-08-29 | 001-scaffold | Spec Kit initialised in-repo (specify 0.16.5) rather than authoring plan/tasks by hand — specs 002-008 need the same artifacts, and ergane parses Spec Kit's `T### [US#]` task shape natively.
- 2026-08-29 | 001-scaffold | Within this spec only, a task is green when every gate that exists at that point passes — the bootstrap exception, because Article VII demands gates that US1 and US3 are the tasks creating.
- 2026-08-29 | 001-scaffold | The smoke harness must accept an externally provided browser and refuse to download one — the spec's assumption that Playwright's install-time download is available is false under the build sandbox, which gives each node a fresh tmpfs HOME.
- 2026-08-29 | operator | The gates workflow installs Chromium on the runner while the engine image bakes it in — same gate, two ways to satisfy the prerequisite, because a runner's HOME persists for the job and an Ergane node's tmpfs HOME does not.
- 2026-08-29 | operator | `package-lock.json` is committed and `npm ci` is the install path — the gates run `npm ci || npm install`, so dropping the lockfile silently degrades every CI run to unpinned resolution while still reporting green.
- 2026-08-30 | 002-map-geometry | The system registry (`src/boot/*`, `src/systems/*`) is implemented as part of US2, adapted from `operator/system-registry` to current main — the plan assumes 001 landed it but it did not, and US2's T018-T021 require it.
- 2026-08-30 | 003-player | `src/player/params.ts` is created complete by US1 and imported read-only by US2/US3 — a single owner keeps tuning from chasing literals across files.
- 2026-08-30 | 003-player | `window.__diag.player` is added by TypeScript module augmentation in `src/player/diag-player.ts` rather than editing `src/diag/diag.ts` — the augmentation route leaves every 001/002 field untouched.
- 2026-08-30 | 003-player | `window.__playerDrive(velX, velZ, ms)` is a synchronous input seam that writes `desiredVel*` then integrates over `ms` — the smoke gate must script a walk before US3's keyboard exists, and a synchronous drive keeps the gate fast and deterministic.
- 2026-08-30 | 004-interaction | US1-S5 and FR-003 are amended in `spec.md` to name the moving refusal per state (`blocked-moving` opening, `refusing-closing` closing) — as written they demanded both names from one closing door, which US1-S6 contradicts and no implementation can satisfy.
- 2026-08-30 | 004-interaction | `MAX_STEP_MS` is 500 — US1-S3 steps in 500 ms ticks and requires them to integrate in full, so a smaller clamp would break the frame-rate independence it asserts.
- 2026-08-30 | 004-interaction | Door gates carry an `interact` / `close` phase, so one registry serves US1's crush gate and US2's lock gate rather than `door.ts` growing a second seam.
- 2026-08-30 | 004-interaction | `openTiles()` reaches 003's collider as a live `ReadonlySet` view, not a snapshot — 003's call site changes by one line and keeps its signature.
- 2026-08-30 | 004-interaction | The doors system hides 002's static `D` faces and builds the doorway's floor, ceiling and jambs itself: 002 treats a door tile as solid, so those faces do not exist and an opened leaf would reveal a hole.
- 2026-08-30 | 004-interaction | US2's one validator edit lands in `src/level-validate.ts`, not `src/level.ts` as plan.md's structure sketch says — 002 split the validator out, and the call line belongs where `validateLevel()` actually lives.
- 2026-08-30 | 004-interaction | A rule declares its own `ErrorCategory` by augmenting `ExtraRuleCategories` from its own file — US3's `secret-placement` then needs no edit to `level-validate.ts`, which is the point of discovering rules by glob.
- 2026-08-30 | 004-interaction | `locks.ts` imports no part of the door machine: the gate's shape is structural, so `registerDoorGate` accepts it while the lock stays a pure question about the inventory.
- 2026-08-30 | 004-interaction | The doors system records *that* a command was refused and the keys system names the key — `lastRefusalKeyKind` is written by the story that owns locks, so `doors/register.ts` is not reopened.
- 2026-08-30 | 004-interaction | US2's worktree merges the US1 branch it is declared to depend on: it was dispatched from a base without `src/interaction/`, and re-authoring US1's outcome union and gate registry would be the cross-story same-file write the plan exists to prevent.
- 2026-08-30 | 004-interaction | US2 was landed by the operator after five consecutive `size_refusal` failures reporting 109,730 bytes against a 65,536 cap — a number matching no computable base (the branch measures 50,725 against `main`). Gates were re-run by hand (typecheck, build, 186 tests) and the same judge model was run manually against US2's acceptance criteria: VERDICT PASS, 8/8 scenarios. Feedback #62/#63.
- 2026-08-30 | 004-interaction | A secret travels at the door's own rate (`SECRET_TILE_MS = DOOR_TRAVEL_MS / DOOR_TRAVEL_TILES`) rather than declaring a second duration in `params.ts`, which US1 owns.
- 2026-08-30 | 004-interaction | `found` latches when travel actually begins, so a secret whose first tile is solid is never counted — `secretsFound` names openings, not presses.
- 2026-08-30 | 004-interaction | `secretRemainingTiles` is added to `InteractionDiagnostics` by augmentation from `secret-field.ts`: US3-S6 wants the shortfall in diagnostics, and US1 owns that file.
- 2026-08-30 | 004-interaction | The secrets system installs its own `keydown` listener resolving through the one `bindings.ts` table, and records a reason only when a secret was in reach: FR-005 binds one command *path*, not one listener.
- 2026-08-30 | 004-interaction | The tile a pushed wall rests on is not re-blocked for collision: the passable-tile registry only adds tiles, and inverting it would mean editing `open-state.ts` and 003's collider, which no US3 task owns.
- 2026-08-31 | 006-enemies | `stepGuard` returns a fresh record and advances only the `Rng` in place, reporting `randomConsumed` per tick — that flag is what lets US1-S9 assert a different seed diverges only where randomness was drawn.
- 2026-08-31 | 006-enemies | Idle patrol draws heading and jitter every idle tick, using the heading only when due — the generator's position then depends on the tick count, never on a branch, which is what makes the trace exact.
- 2026-08-31 | 006-enemies | `StepContext.doorStates` is 004's open-tile key set, not a second door-state shape — `openTiles()` produces it and `isTileBlocking` consumes it already.
- 2026-08-31 | 006-enemies | A chasing guard moves only along a path the injected world returned — a discarded stale path then costs one throttle interval of standing still, not a beeline to nowhere.
- 2026-08-31 | 006-enemies | The guard record splits into `src/enemy/guard.ts`, one module beyond plan.md's sketch — `step.ts` crossed the 400-line ceiling, and Article IV makes the split part of the task.
- 2026-08-31 | 005-materials | `generateMaterial(spec, size)` is the pure function FR-003 is stated over and `generateAlbedo(name, size)` is the memoized entry point over the table — one function cannot be both byte-reproducible under an arbitrary seed and cached per name.
- 2026-08-31 | 005-materials | A seed is varied through `reseed(spec, seed)` in `table.ts`, not by spreading a spec at the call site — `MaterialSpec` is a discriminated union, and a spread loses the correlation between a material's name and its own parameter block.
- 2026-08-31 | 005-materials | `MEAN_DISTINCTNESS_THRESHOLD` is 8 channel units while the five materials are tuned to a smallest measured gap of ~23 (brick 80, blood-stone 57, wood 108, stone 149, steel 182) — the threshold is the floor a retune must clear, not the spread itself.
- 2026-08-31 | 005-materials | The degradation path of FR-007 ships a flat normal *and* `FALLBACK_ROUGHNESS` together whenever either derivation fails: FR-007 names one degraded state, so a half-derived surface — a real normal beside a constant roughness — is never a shipped outcome, and the albedo is applied on both paths.
- 2026-08-31 | 005-materials | The normal encoding is `(-dh/dx, +dh/drow, 1)` normalised, green pointing up the image, because three.js reads tangent-space maps in the OpenGL convention — the one place the sign is chosen, so a test can hand-compute against it.
- 2026-08-31 | 005-materials | `deriveRoughnessMap` centres the height field on its own mean before mapping it into the material's declared band, so a mean lands on that band's midpoint whatever the pattern looks like — `steel < wood < stone` is then a property of `table.ts`, not of a tuned constant in `roughness.ts`.
- 2026-08-31 | 005-materials | `maps.ts` takes its two derivations as an injected `MapDerivations` rather than importing them at the call site, because FR-007's failure path has no natural trigger in a passing build and would otherwise be unreachable from `npm run test`.
- 2026-08-31 | 005-materials | `MaterialDiagnostics` state lives in `diagnostics.ts` and is attached to `__diag` by reference, because the map set is built by pure code with no renderer context to write into; US3 and US4 fill their fields through `publishMaterialDiagnostics()`.
- 2026-08-31 | 006-enemies | `findPath` returns `cells` beginning at the start cell rather than just after it — US2-S1 asks for a list "from start to goal", and a guard already standing on `cells[0]` arrives on it the same tick and slices it, so `step.ts` needs no change.
- 2026-08-31 | 006-enemies | `MAX_NODE_EXPANSIONS` is 4096, one 64x64 grid of cells: a search may cover the whole level exactly once and no more, so the cap is a real bound without ever refusing a route the level actually has.
- 2026-08-31 | 006-enemies | A* orders its open set by (f, h, cell index, insertion sequence) — a *total* order, which is what makes US2-S9's "two identical calls, identical paths" a property of the code rather than of the heap's history.
- 2026-08-31 | 006-enemies | A diagonal corner is passable to sight when at least one of the two cells flanking it is open; only two walls close it, which is US2-S7's pinwheel and nothing wider.
- 2026-08-31 | 006-enemies | `MAX_LOS_STEPS` is 512 — four times the longest span a 64x64 grid can produce, so the bound is unreachable in play and absolute in principle.
- 2026-08-31 | 006-enemies | The `Navigator`'s throttle is a second belt over `step.ts`'s own: a suppressed request returns the cached path and is counted, so a guard asking every tick shows up in `NavReport` rather than in the frame time.
- 2026-08-31 | 006-enemies | A claim reserves a guard's destination cell only, never its route — two guards may share a corridor, and only the cell they would grind against is made exclusive.
- 2026-08-31 | 006-enemies | US1's purity check treats the bare word `navigator` as a DOM global, so `nav.ts` names its own type `Navigator` in prose; the gate is right that `navigator` is a global, and renaming the comment is cheaper than narrowing the check.
- 2026-08-31 | 006-enemies | The falloff curve is piecewise-linear between its breakpoints and clamped outside them, so `damageAtDistance` is total and never mints a zero, a negative or a NaN — a shot past the last breakpoint stings for the minimum rather than falling off a cliff.
- 2026-08-31 | 006-enemies | The curve's declared range (1..16 cells) contains `ATTACK_RANGE_CELLS` (6) with room to spare, so every shot a guard can actually take is evaluated on a real segment of the table and never on its clamped tail.
- 2026-08-31 | 006-enemies | "No shot emitted" is a *state* question, not a geometry one: `resolveShot` answers null for every state but `attack`, because a wall between guard and player is `blocked`, and US3-S5's "no shot at all" is the guard never having reached `attack`.
- 2026-08-31 | 006-enemies | `attack.ts` walks its own DDA rather than having `los.ts` publish where a refused trace died: a shot needs a termination distance sight has no reason to carry, and a scratch buffer on US2's module would have made a sibling's file pay for this story. The two walks are swept against each other over a field of pillars, so they are held identical by a test rather than by a shared line.
- 2026-08-31 | 006-enemies | A spawn marker on a wall cell is recorded as a named error and the guard is still instantiated: dropping it would make the live count disagree with the marker count, and throwing would tell a reader "something crashed" instead of naming the coordinates.
- 2026-08-31 | 006-enemies | Spawn faults go to `__diag.enemySpawnErrors` rather than `__diag.errors`: 001 owns that array and its meaning is "something threw", so `tools/smoke-checks/enemies.mjs` fails the gate on the new one and the consequence is identical with the ownership clean.
- 2026-08-31 | 006-enemies | Guard AI runs at a fixed `GUARD_TICK_MS` with at most `MAX_TICKS_PER_FRAME` steps of catch-up and the rest of the backlog dropped — a stalled tab must not be able to come back and run four hundred ticks of pathing in one frame.
- 2026-08-31 | 006-enemies | `pathable` lives on the world's record, not on the `Guard`: an unreachable answer outlives the tick that asked, so the guard is put back to `idle` on that tick and stays reported unpathable until a later request actually returns a route.
- 2026-08-31 | 006-enemies | `world.ts` re-states the four fields of `step.ts`'s idle entry rather than opening US1's file to export `onEnter` — a duplicated literal pinned by a test costs less than a cross-story edit to the module the whole spec's ordering rests on.
- 2026-08-31 | 006-enemies | The smoke-check loop lives in `tools/smoke-check-runner.mjs`, not inside `tools/smoke.mjs`: that harness is already past the 400-line ceiling and this story is not the one that splits it, so the shared file grows by ten lines instead of seventy.
- 2026-08-31 | 006-enemies | `view-angle.ts` measures bearings in the codebase's own yaw convention (`atan2(-dx, -dz)`, as `step.ts` and `camera.rotation.y` do) rather than a clockwise compass, because a bearing needing reflection before subtraction from a facing would show a guard's back as it turned to face the player and no distinctness test would catch it.
- 2026-08-31 | 006-enemies | The billboard turns about Y alone, so it faces the camera horizontally and stays upright: a full `lookAt` would lay the sprite back as the player looked down.
- 2026-08-31 | 006-enemies | The guard material is an alpha *cutout*, not a blend, because three.js draws a double-sided transparent material in two passes — twice US4-S7's budget of one draw call per guard.
- 2026-08-31 | 006-enemies | `enemySprites` reaches `__diag` by module augmentation from the billboard system as an optional field, because US3 already made this spec's one edit to `src/diag/diag.ts`.
- 2026-08-31 | 006-enemies | The harness's orbit overrides the *camera* at order 65 and never moves the player, because `step.ts` snaps an alerted guard's facing at the player each tick, so orbiting the player would circle a guard pivoting to keep its front to the viewer — the one arrangement in which eight bearings cannot yield eight columns.
- 2026-08-31 | 006-enemies | A culled guard still has its bearing computed and published, because `viewAngle` is its relation to the viewer and stays true unseen, while `visible` is what the frame drew.
- 2026-08-31 | 006-enemies | The update that first sees `death` renders the first death frame rather than adding that frame's delta to the clock, so a slow frame cannot step over the start of a four-frame animation.
- 2026-08-31 | 007-combat-hud | A shot's distance is where the ray meets the guard's hit cylinder, not the guard's centre, because that is the number the wall comparison must be made against.
- 2026-08-31 | 007-combat-hud | Fire credit past one interval is dropped, not carried, so standing still cannot be cashed in as a burst; an ammo refusal instead leaves the timer standing, so a magazine refilled next frame fires at once.
- 2026-08-31 | 007-combat-hud | FR-002's "no literals at call sites" is enforced by scanning every importer of the weapon table, exempting values below ten without a decimal point, because an ammo cost of one is indistinguishable from an array index and a stated exemption is honest where a silent one would not be.
- 2026-08-31 | 007-combat-hud | FR-011's cross-spec reset goes through one additive accessor per owning system, because doors, secrets, keys and guards keep live state in module locals with no reader; the reset policy stays in `reset-adapters.ts`.
- 2026-08-31 | 007-combat-hud | The enemies system rebuilds its world on restart rather than rewinding it: the tick accumulator and queued damage are closure-private, and a rewind would land a pre-restart shot on a post-restart guard.
- 2026-08-31 | 007-combat-hud | A collected key's mesh is hidden, not removed, so restart can put it back within FR-018's draw-call budget.
- 2026-08-31 | 007-combat-hud | The restart command is serviced once per frame rather than on arrival, so two presses in quick succession are one reset.
- 2026-08-31 | 007-combat-hud | `snapshotRunState` is a flat dotted map of run state and not presentation, so SC-002's comparison is a key walk and the exempt set is a list `008` extends in one place.
- 2026-08-31 | 007-combat-hud | Kills score before damage applies within the vitals frame, so a guard killed on the tick its shot kills the player still scores and the death counts once.
- 2026-08-31 | 007-combat-hud | Each reset adapter republishes its own `__diag` fields, so SC-002's two snapshots are both taken at zero simulation age — setup, and the frame the reset completed. "One frame after" is not an age: guards step on a 50ms accumulator, so one frame is zero ticks when frames are quick and up to four when they are slow, and the comparison would fail on a slow machine over 006's live behaviour rather than a leak.
- 2026-08-31 | 007-combat-hud | `pickups.ts` decides *whether* a pickup is collected and `pickup-effects.ts` decides *what* it does, joined by an `apply` callback that returns whether it took the pickup — so the collection path never learns what health is, and "a full player does not destroy supplies" is one refusal rather than a special case threaded through the radius test.
- 2026-08-31 | 007-combat-hud | The collection radius is 0.6 tiles measured radially from the pickup's tile centre, not the tile-containment test 004's keys used: a declared radius is what FR-014 asks for, and a radial one means the diagonal corner of a tile is out of reach where an axis-aligned box would have said otherwise.
- 2026-08-31 | 007-combat-hud | A marker with an undeclared kind instantiates no pickup and is not counted in `pickupsTotal`, because a pickup that cannot exist can never be collected and counting it would make US3-S10's `pickupsCollected === pickupsTotal` unreachable. This is the opposite of 006's spawn-fault choice, and deliberately: a guard on a wall can still be fought, an item of no kind cannot be picked up.
- 2026-08-31 | 007-combat-hud | Marker faults are published to `__diag.pickupErrors` by module augmentation from the pickups system, following 006's `enemySpawnErrors` precedent — `__diag.errors` means "something threw", `src/diag/diag.ts` is not edited, and a load fault is not combat state so it is not a field of FR-018's `__diag.combat` either.
- 2026-08-31 | 007-combat-hud | 004's keys system no longer collects: its `collectAtPlayer` is deleted and a key reaches the inventory as a `Pickup` with a key kind whose effect is `addKey`. The inventory, the meshes and `KeyPickup.consumed` stay 004's, so this is one collection path with one owner per piece of state rather than a second mechanism (US3-S9).
- 2026-08-31 | 007-combat-hud | Every drawn pickup shares one `InstancedMesh` coloured per instance, and a consumed one is scaled to zero rather than removed: a mesh each would have been a dozen draw calls out of the twenty US4 still has to fit a HUD, a view-model and a muzzle flash into.
- 2026-08-31 | 007-combat-hud | The pickups system runs at order 74, before vitals at 75, for two reasons that both bite: health and score moved by a pickup are published in the same frame rather than a frame late, and vitals' setup captures SC-002's spawn snapshot, which must already carry `pickupsTotal`.
- 2026-08-31 | 007-combat-hud | Collection is behind `commandsResolve()`, so a dead player collects nothing and the supplies they died on are still there after the restart.
- 2026-08-31 | 007-combat-hud | The HUD is one canvas-2D texture on one screen-space quad parented to the camera, and the HUD system adds the camera to the scene itself: `main.ts` never added it, and adding it from a system is what the registry exists for.
- 2026-08-31 | 007-combat-hud | The muzzle-flash clock is elapsed-since-the-shot plus a lit flag, not a decrementing remainder — US4-S7 asks for *exactly* zero, and a remainder stepped by a hundred float deltas can settle a whisker above it.
- 2026-08-31 | 007-combat-hud | The HUD publishes what it composited through `window.__hud.drawn()`: US4-S2 and US4-S3 are claims about *displayed* values, and a texture cannot be read back as text.
- 2026-08-31 | 008-polish | The elevator's refusal at zero health is the declared outcome `exit-refused-dead`, added to 004's one outcome set beside `exit-used` and `already-exiting`: FR-001 requires the refusal but names no word for it, and 004's rule is that no command resolves to an empty or silent result.
- 2026-08-31 | 008-polish | Reach is asked before health and before the run state, so a press from across the room is `no-target` whatever the run is doing and the elevator never speaks for a press aimed at a door.
- 2026-08-31 | 008-polish | A run in `exiting` takes no further damage, though the guards outside keep moving and firing: a lethal shot landing during the lift's travel would otherwise leave a run `dead` and `complete` at once, which is the "won and lost" the Edge Cases forbid. The shot that lands *before* the press still refuses the exit (US1-S6).
- 2026-08-31 | 008-polish | `complete` is answered `already-exiting` too — the elevator has been used and does not re-trigger — rather than a fourth outcome for a lift that has already arrived.
- 2026-08-31 | 008-polish | The elevator travel is compared against its duration with a 1e-9-second tolerance, because accumulating 2000 additions of 0.001 lands short of 2 and without it the lift would need one further millisecond at 1 ms deltas and none at 500 ms — the exact disagreement US1-S3 forbids.
- 2026-08-31 | 008-polish | `src/run/state.ts` holds the live run in a module local, in the shape `player/state.ts` and `combat/run-state.ts` already established, so 006's and 007's systems consult one phase through `currentRunState()` instead of importing the elevator system.
- 2026-08-31 | 008-polish | The rating is the mean of the kill, secret and treasure percentages against a five-band table (`LEGEND` 100, `ACE` 80, `VETERAN` 55, `SOLDIER` 30, `ROOKIE` 0 as the floor), because FR-005 names no bands and a mean makes US2-S4's perfect run a property of the table rather than a special case; an axis with no denominator is excluded from that mean rather than scored zero, since a run cannot be faulted for missing secrets that do not exist.
- 2026-08-31 | 008-polish | The zero-denominator placeholder is `--` and `percentOf` answers `null` rather than `NaN`, so the refusal happens where the division would, not where the text is drawn.
- 2026-08-31 | 008-polish | The screen adds `/`, `%` and `-` to 007's stroke table and draws through `compose.ts`'s `drawGlyphText`, so US2-S5 is one table and one renderer.
- 2026-08-31 | 008-polish | `completions` counts the `complete` transition `stepRun` returns rather than a frame on which the state reads `complete`, because a state watcher needs a flag the reset can forget to clear; it stays out of the reset registry entirely and `RESTART_EXEMPT_FIELDS` declares that.
- 2026-08-31 | 008-polish | The elevator system publishes that transition as `getLastRunTransition()`, so the stats screen counts a `complete` made at order 80 in the frame it happened without the run-state machine being reopened.
- 2026-08-31 | 008-polish | The screen publishes what it composited as `window.__run.lines()`, following 007's `window.__hud.drawn()`, because US2-S1 and US2-S2 are claims about displayed values and a texture cannot be read back as text.
- 2026-08-31 | 008-polish | Restart from the stats screen is `requestRestart()` and nothing else, the run timer returning to zero because US1 registered `resetRunTimeline` as a resettable rather than because this story put a field back.
- 2026-08-31 | 008-polish | The gate discovers its browser by walking every `chromium*` build under `PLAYWRIGHT_BROWSERS_PATH` rather than by version-stamped paths that had to be bumped by hand; it still never downloads and never skips.
- 2026-08-31 | 008-polish | Every sound is a row in `src/audio/sound-table.ts` — duration, envelope, layers and seeds — that `synth.ts` renders to a `Float32Array` at a supplied sample rate, because Constitution II leaves a sound no file to be: a sound *is* its parameters, each row's layer gains summing to one so a sample is bounded by `peak * gain` by construction rather than by a hope about mixing.
- 2026-08-31 | 008-polish | Sounds answer resolved events — gunfire the delta of 007's `shotsFired`, which excludes refusals; a door the moment a leaf *starts* moving; a footstep distance actually travelled, capped at twice a frame of sprinting so a restart's jump to spawn buys no burst — under a declared cap of 12 voices and a ceiling of 0.7, the bus dividing the budget whenever the live voices sum past it.
- 2026-08-31 | 008-polish | The context is asked to suspend even when the platform hands one back running, so `suspended` before a gesture is a fact about `src/audio/context.ts` rather than about an autoplay policy; a fallback reads `<sound-id>: <reason>, that event is silent` or `context: <reason>, every sound is silent`, matching `__diag.audio.fallbacks`, and none was taken in this build — all six sounds synthesized, the context constructed — so this line records the vocabulary rather than an omission.
- 2026-08-31 | 008-polish | `ssao: webgpu, three's node ambient occlusion needs an MRT normal pass this chain does not build` — the one effect disabled on a backend in this build (FR-016, US4-S8), and the exact entry `src/post/node-chain.ts` records in `__diag.post.fallbacks`; the other three effects and the scene render on WebGPU regardless.
- 2026-08-31 | 008-polish | Post-processing choices the spec leaves open: motion blur is an accumulation blur because three ships no per-object velocity pass; bloom and grain default on and ambient occlusion and motion blur off, the first two being cheap and identical on both backends; the intermediate buffer is 8-bit sRGB and AO and bloom run at half and a quarter of the viewport, because nothing in this scene is brighter than white and the fill saved is what keeps the default page above 001's floor; a fresh chain renders one frame at build time, so a chain that cannot render is caught where its reason can be recorded rather than as a black screen; and `__diag.drawCalls` keeps meaning the *scene's* calls behind a probe pass while the HUD composites above the effects and the muzzle flash goes through them, because 001's budget must not silently become "under twenty full-screen quads" and a grain-covered ammo counter is not a readout.
- 2026-08-31 | 008-polish | All four post effects default *off*, each one key away — reverses the "bloom and grain default on" clause of the entry above, because FR-017 promises 001's frame-rate floor with the four disabled while 001's harness asserts that same floor against the page's defaults, and the two agree only when the defaults are the disabled state: measured under a two-core software rasterizer, bloom and grain on took the first read after `ready` from 15 fps to 4 and put the page under the floor it inherited.
- 2026-08-31 | 008-polish | The WebGPU fallback line above names `src/post/node-chain.ts`, which is now `src/post/chain.ts` — the two backends were folded into one module under Article IV's ceiling; the entry it records in `__diag.post.fallbacks` is unchanged.
- 005-materials/US3: `__diag.materials.untexturedMeshes` counts the level surfaces this spec skins (002's walls/floor/ceiling, 004's doors/secrets/recesses), classified independently of whether the skinning succeeded — 006's enemy sprites, 007's pickups and its weapon viewmodel are other specs' meshes and texturing them with a wall material would be a worse defect than leaving them alone.
- 005-materials/US3: secret blocks take the material of the wall they are embedded in rather than a distinguishing one, because 004 builds them in their neighbour's colour so "an unpushed secret is indistinguishable from it"; making them stand out would regress 004's own acceptance criterion, and FR-008 only requires that a secret carry a declared material.
- 005-materials/US3: the floor takes `wood` and the ceiling `blood-stone`; all five wall type IDs are in use in the shipped grid, so floor and ceiling cannot avoid every wall material, but they avoid the dominant type '1' (252 tiles) and each other. Doors are forced to `stone` — it is the only material no door-adjacent wall type uses.
- 005-materials/US3: frame deltas in `src/main.ts` are taken from `performance.now()` at callback entry instead of the timestamp `requestAnimationFrame` passes in. Same clock, but the rAF argument is the *browser frame's* start time, and a frame begun before a long synchronous load keeps that start time when its callbacks finally run — so with five 512px materials generated at setup (281ms measured headlessly, inside the 1500ms FR-004 budget) the second frame's delta came out as the whole load, ~466ms against a 16.7ms steady state. That single sample is handed to every system as `deltaMs` and pulled `__diag.fps` — read two frames after `ready` — to 4.1, under 001's floor of 5, on a level rendering at 60. This is the same artifact the 008-polish entry above works around by defaulting its effects off ("15 fps to 4"); measuring entry-to-entry fixes it at the source, and stops frame two integrating half a second of player and door motion. `src/main.ts` is the only file this spec touches outside its own modules, and it is a change to the clock rather than to the wiring the no-edit rule exists to protect.
- 2026-09-01 | operator | WebGL is the default backend and WebGPU is opt-in behind `?webgpu`, amending 001's FR-003 from a MUST that chose WebGPU wherever `navigator.gpu` existed — because that MUST shipped a black screen to every WebGPU-capable visitor of the published site, and because WebGL is the only backend any gate has ever executed: the harness runs headless Chromium, which exposes no `navigator.gpu`, so all 778 tests and 8 smoke checks across every merge-group build in this repo's history verified a path most users never take while the path they do take had never drawn a verified frame. Filed as `gates/headless-gate-cannot-execute-the-default-runtime-path`.
- 2026-09-01 | operator | The WebGPU black screen was a sampler three r167 never complains about: `MATERIAL_ANISOTROPY = 8` on textures keeping `DataTexture`'s default `NearestFilter`. WebGL tolerates that pair; WebGPU forbids `maxAnisotropy > 1` with a non-linear filter, so Dawn invalidated the sampler, every bind group referencing it, and the whole render pass — every frame, with no exception, no console output and no fallback, `__diag` reading `renderer=webgpu, errors=[], drawCalls=0`. The renderer also caches the invalid sampler, so the failure is sticky. `src/post/render-hook.ts` falls back when a chain *throws*; a chain that renders zero draws successfully never throws, which is why nothing caught it.
- 2026-09-01 | operator | Fixed that sampler by lowering `MATERIAL_ANISOTROPY` to 1 rather than by switching the textures to trilinear filtering. Trilinear + anisotropy 8 was the first cut, is truer to FR-011 as originally written, and rendered correctly — but the merge-group gate vetoed it honestly: the CI runner rasterizes headless WebGL in software, and trilinear sampling of five 512px textures took the normal smoke pass to 3.9 fps against a floor of 5, which US4-S6 forbids lowering. With nearest filtering the old anisotropy of 8 was visually inert anyway (anisotropy refines mip sampling and a nearest min filter never reads the mipmaps, so `generateMipmaps = true` has always been a no-op here), so 1 changes nothing a player sees and unbreaks WebGPU. FR-011's mipmap clause and US4-S5's anti-aliasing clause are amended to KNOWINGLY UNMET rather than left as claims the code contradicts; restoring them needs cheaper derivation first, not just different filters.
- 2026-09-01 | operator | The game is published to GitHub Pages from `main` by `.github/workflows/pages.yml`, deliberately outside the `ergane.yaml` gate set: the workflow triggers only on `push: main` and `workflow_dispatch`, never `pull_request` or `merge_group`, so it adds no required check and cannot block or slow the merge queue. The `/wolfenstein/` base path is injected at deploy time via `npm run build -- --base=` and must never move into `vite.config.ts`, because `tools/smoke.mjs` serves `dist/` at the root and a baked-in base would 404 the bundle and fail the required `test` gate. No spec covers deployment at all, so this invariant currently lives only in that workflow's comment and in this line; filed as `spec/shipped-surface-outside-every-spec-is-ungated`.
- 2026-09-02 | operator | `CONTEXT.md` is created at the repository root as the project's glossary — what the words mean and which word to use when several would do — because eight specs have accumulated vocabulary that no single spec's `## Key Entities` owns, and 009 adds a second domain (verification artifacts) whose terms cross every spec. This does not reopen the ADR question settled at the head of this file: that precedence note forbids `docs/adr/` and says nothing about a glossary, decisions still land here as one-liners, and `CONTEXT.md` carries no decision, requirement or implementation detail.
- 2026-09-02 | 009-playtest-uat | `npm run play` runs headed on the operator's display and refuses to run without one, and is deliberately absent from `ergane.yaml` — a headless recording is the artifact misrepresenting itself, since the gate's FPS floor is 5 precisely because headless Chromium rasterizes this game in software, and a tool whose entire output is a video must not ship footage that lies about the frame rate. The cost is accepted knowingly: this is the first spec in the repository that no gate executes.
- 2026-09-02 | 009-playtest-uat | The playthrough drives the game through real input only — pointer lock from a real canvas click, `keydown`/`keyup` on the codes `src/player/keyboard.ts` binds, mouse movement under the lock, `src/interaction/bindings.ts`'s interact codes, and the real fire and weapon bindings — and adds no seam under `src/`, because all three unknowns were measured on the built page rather than assumed: a synthesized `keydown('KeyW')` held 700 ms moved the player `z 10.5 → 8.35`, which is 2.15 units at `WALK_SPEED` 3 through the real collider; a click on `#game-canvas` *is* granted pointer lock (`document.pointerLockElement === "game-canvas"`); and synthesized mouse movement under that lock turns the camera (`yaw 0 → −0.216 rad` over 360 px, ≈0.0006 rad/px). A `window.__playerLook` beside `window.__playerDrive` would have been a second input path only the harness uses, and the whole value of the story is that there is not one — this command is now the only thing in the repository that executes the input layer a player actually touches.
- 2026-09-02 | 009-playtest-uat | A recording is build output in the sense `dist/` is, not a repository asset in the sense Constitution II forbids: the output directory is excluded twice — added to `.gitignore` alongside `*.webm` and `*.mp4`, and added to the skip list in `tools/check-no-binaries.mjs` beside `node_modules`, `dist` and `.git`. The walker's *forbidden-extension* list is deliberately not extended, because forbidding `.webm` there would fail the smoke gate on the runner's own output; instead FR-005 makes "`npm run smoke` passes with the output directory populated" an acceptance criterion, so the exclusion is proved rather than asserted.
- 2026-09-02 | 009-playtest-uat | The verdict is two-tier — reaching `complete`, an empty `__diag.errors` and no uncaught page error fail an attempt; completion percentages, rating, frame rate and duration are measured, reported and never fatal — because a UAT that goes red on the hardest optional objective is a UAT nobody runs twice, and the question this artifact answers is "is the game broken", not "did this run go well".
- 2026-09-02 | 009-playtest-uat | An attempt is retried only on a hard failure or a harness fault, to a maximum of three, and the number used is stated inside the verdict itself. Guards react to a live player on timing this spec does not control, so a single attempt conflates a regression with a bad roll; the known cost of best-of-three — a difficulty regression hiding behind a lucky third try — is paid off by making the attempt count part of the result rather than a detail in a log, so a pass that took three tries can never read as a pass that took one.
- 2026-09-02 | 009-playtest-uat | One output directory, replaced per invocation rather than accumulating timestamped runs, and assembled in a temporary location and moved into place only once the invocation finishes — history is traded away for a single obvious path, and the swap is what stops an interrupted run leaving a half-written directory that reads as a result.
- 2026-09-02 | 009-playtest-uat | Turning the camera is closed-loop with an adaptive yaw-per-pixel estimate rather than an open-loop "move N pixels", because two calibrations of the same page disagreed by more than 3×: 30 synthesized moves of 12px measured 0.0006 rad/px and 40 moves of 100px measured 0.0020, so any single constant is wrong at the other step size. The virtual cursor is also never re-centred — measured to x=4100 in a 1280-wide viewport, moves past the edge keep producing deltas at exactly the same rate, and a re-centring move would itself be a delta, which is to say a turn nobody asked for.
- 2026-09-02 | 009-playtest-uat | The walker reads health before it blames geometry. `commandsResolve()` zeroes desired velocity on death, so a held key stops producing movement and a caller watching only position cannot tell a corpse from a wall: the first run of this harness reported "stopped advancing at (55.43, 50.54)" in the middle of an empty room when the truth was that a guard had killed the player two tiles from the lift. `readPlayer` therefore returns `health`, `dead` and the run state alongside the position, and a leg that stops while dead says so.
- 2026-09-02 | 009-playtest-uat | US1 routes to the exit with the game's own A*, which crosses no locked door at all — 91 cells, needing neither key. This is worth writing down because it contradicts the only other route in the repository: the hand-authored one in `tools/smoke-checks/run.mjs` goes through the silver-locked door at (42,31) and collects the key at (30,30) to do it. Both are correct; the gate's route was written to exercise the lock, and the playtest's is whatever the pathfinder returns. A future reader comparing them should not conclude one of them is broken.
- 2026-09-02 | 009-playtest-uat | The route is sprinted, Shift held for its whole length, and the margin is thin by measurement rather than by guess: walked at 3.0 units per second the unarmed agent is killed before the lift, and sprinted at 5.4 the same route arrives with 35 health of 100. US1 therefore completes the level but has no margin against a bad guard roll — which is not a defect to fix here but the exact thing US2's FR-007 (return fire) and US4's retry budget exist for, and is the first evidence that the two are load-bearing rather than decorative.
- 2026-09-02 | 009-playtest-uat | `esbuild` is promoted from a transitive dependency of `vite` to an explicit devDependency, pinned at the ^0.21.5 already installed. It compiles `tools/play/nav-entry.ts` so the runner can route with the game's own `findPath` (Constitution I: named here and in the diff that adds it). Depending on it transitively would have worked today and broken silently the first time vite changed its own bundler — an unnamed dependency is still a dependency.
- 2026-09-02 | 009-playtest-uat | US1 answers the guards that engage it rather than walking through the firefight unarmed, and FR-007 moved from US2 to US1 to say so. The original split put all combat in the objective story, which conflated two different things: shooting the guard currently shooting at you, and hunting down all eight. The first is part of *crossing* the level and the second is part of *clearing* it — and an agent that carries a loaded pistol past three guard markers without firing tests less of the game than it runs, never resolving a hitscan, moving the kill counter or changing the HUD. Measured: unarmed and sprinting the run finished on 35 health of 100 with a ROOKIE rating; answering fire it finishes on 69 with 6 of 8 guards dead and a SOLDIER rating.
- 2026-09-02 | 009-playtest-uat | `src/enemy/world.ts` publishes each live guard's `x` and `z` on its `__diag.enemies` record, additively over the shape 006's FR-011 declared, and the two tests asserting that record's exact key set were updated to match. Aiming needs a bearing and none was published: `viewAngle` is the sprite column, which is a guard's facing *relative to the camera* rather than the direction to it, and `__enemySprites.orbit` moves the camera rather than reporting anything. This is the extension Constitution III asks for — "extend that harness's assertion surface when a story needs a runtime fact it cannot yet report" — and it is the opposite kind of change from the `window.__playerLook` this spec refused: perception may be extended, input may not. The claim worth protecting was never "009 touches no file under `src/`"; it was "the agent plays through the input a player uses".
- 2026-09-02 | 009-playtest-uat | A headed run owns the operator's physical mouse, because the pointer lock is real: a human moving their own mouse mid-run feeds `movementX` into the same page the agent is steering, and the closed-loop turn cannot converge against it. Observed on 2026-09-02 as "the camera did not reach yaw 0.585 within 240 frames". This is classified as a `HarnessFault` — the browser and the desk, not the game — so it is retryable under FR-016 rather than being reported as a gameplay failure, and the README says to leave the machine alone while it plays. It is the unavoidable price of D1's headed-only decision: the alternative is a nested display, which would cost the GPU that decision exists to use.
